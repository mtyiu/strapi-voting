const PERMISSIONS = {
  // Main navigation
  accessMain: [{ action: 'plugin::voting.menu.access', subject: null }],
  // Settings
  settingsAccess: [{ action: 'plugin::voting.settings.access', subject: null }],
};

export default PERMISSIONS;
